/**
 * A simple message box to send messages to the player.
 *
 * @author  Ikaros Kappler
 * @date    2023-06-28
 * @version 1.0.0
 */
export declare class MessageBox {
    private domElement;
    private currentTimerKey;
    constructor();
    showMessage(message: string): void;
}
